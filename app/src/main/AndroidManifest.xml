<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mydiary.bloomydev.ilasisi.mydiary"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="16" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="mydiary.bloomydev.ilasisi.mydiary.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/oneNote"
            android:name="mydiary.bloomydev.ilasisi.mydiary.DetailsActivity">
        </activity>
        <activity
            android:label="Add New Note"
            android:name="mydiary.bloomydev.ilasisi.mydiary.AddNote">
        </activity>
        <activity
            android:label="@string/app_name"
            android:name="mydiary.bloomydev.ilasisi.mydiary.MainActivity">
        </activity>
    </application>

</manifest>